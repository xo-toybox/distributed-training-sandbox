# Modal + tracing configuration (paths are resolved relative to this file).
MODAL_TRACE_VOLUME=ddp-trace-vol
MODAL_GPU_SPEC=A10G:2
TRACE_OUTPUT_DIR=./ddp_traces
TENSORBOARD_PORT=6006

# Training script output (required by ddp.py when running locally).
DDP_TRACE_DIR=./ddp_traces/local_run
